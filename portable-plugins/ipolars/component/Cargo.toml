[package]
name = "ipolars-portable-component"
version = "0.1.4"
edition = "2024"
license = "GPL-2.0-or-later"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.55.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
